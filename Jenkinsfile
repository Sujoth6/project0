
stages {
    stage('Build') {
        steps {
            echo 'Building the app'
        }
    }
    stage('Test') {
        steps {
            echo 'Running tests'
        }
    }
    stage('Verifying') {
       steps {
            echo 'Verifying the test'
       }
     }
    stage('Deploy') {
        steps {
            echo 'Deploying the app'
        }
    }
}
